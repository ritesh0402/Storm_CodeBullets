import {
   Box,
   Button,
   Card,
   CardActions,
   CardContent,
   CardMedia,
   Tooltip,
   Typography,
   useMediaQuery,
   useTheme,
} from "@mui/material";
import React from "react";


const EventCard = ({ event, isPast }) => {


   return (
      <div>
         hello
      </div>
   );
};

export default EventCard;
